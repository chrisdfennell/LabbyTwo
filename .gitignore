# Real environment file — never commit
.env

# Local Compose customisation: extra networks, the Docker socket, host mode. Yours, not
# the project's, and the installer must never replace it.
docker-compose.override.yml

# Build output
bin/
obj/
publish/

# The database, the keyring, and every credential you have entered
data/

# IDE / tooling
.vs/
.vscode/
*.user
*.suo

# Test output
[Tt]est[Rr]esults/

# Docker image exports
*.tar
